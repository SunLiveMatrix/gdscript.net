extends Node2D


# Called when the node enters the scene tree for the first time.
func _ready():
	print("TForm: GDScript")
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta):
	# TODO: delta is the elapsed time the previous frame.
	pass


func _draw():
	var _apply_scale: Array = [512, 612, 712, 812, 912]
	var _get_angle_to: int = 512
	var _global_position: Object = Object.new()
	# check template motion evolution scripts event classify ordering
	if _apply_scale && _get_angle_to:
		var _global_scale: int = 512
	else:
		while _apply_scale:
			continue
















 



	 				 
