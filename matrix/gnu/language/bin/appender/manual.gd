extends manual

# This is the default implementation of the interface that is used by the standard library
# and is responsable to be overriden by the standard library itself.

interface secondary extends manual implementation implements secondary {
    class _Admire extends Reference
        func _AdmireObject(_args):
            _args {} && return _args.value()
            pass
        
}
