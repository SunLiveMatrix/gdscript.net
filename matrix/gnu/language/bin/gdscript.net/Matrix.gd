extends Window


# Called when the node enters the scene tree for the first time.
func _ready():
	print("TForm: Matrix")
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta):
	# TODO: delta elapsed time since the previous frame
	pass

class _db_to_linear:
	func _get(_property):
		_property = [512, 612, 712, 812, 912]
		# list porperty method easy constructor development
		var _db_to_property_types: EditorCommandPalette = Object.new()
		var _db_to_property_objts: Object = Object.new()
		var _db_to_property_event: EditorResourcePreview = Object.new()
		var _db_to_property_mapse: Semaphore = Object.new()
		var _db_to_property_lists: Semaphore = Object.new()
		var _db_to_property_forms: EditorProperty = Object.new()
		if  _property && _db_to_property_event:
			print("TForm: Property")
		elif _property:
			while true:
				continue
		else:
			return _property
		if  _property && _db_to_property_forms:
			print("TForm: Property")
		elif _property:
			while true:
				continue
		else:
			return _property
		if  _property && _db_to_property_lists:
			print("TForm: Property")
		elif _property:
			while true:
				continue
		else:
			return _property
		if  _property && _db_to_property_mapse:
			print("TForm: Property")
		elif _property:
			while true:
				continue
		else:
			return _property
		if  _property && _db_to_property_objts:
			print("TForm: Property")
		elif _property:
			while true:
				continue
		else:
			return _property
		if  _property && _db_to_property_types:
			print("TForm: Property")
		elif _property:
			while true:
				continue
		else:
			return _property
					
